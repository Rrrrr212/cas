{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "cas-ha-observability.serviceAccountName" . }}
  labels:
    {{- include "cas-ha-observability.labels" . | nindent 4 }}
{{- end }}
