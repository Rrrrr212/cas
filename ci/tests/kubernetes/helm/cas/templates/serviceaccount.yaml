apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "cas.serviceAccountName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "cas.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}